extends default

block content
    div.recipe
        div.recipe-image(style=`background: url('/recipes-and-stuff/images/${recipe.imgSrc}') top center
        no-repeat; background-size: cover;`)
        div.recipe-details
            h1.recipe-name
                | #{recipe.name}
            p.recipe-description
                | #{recipe.description}

